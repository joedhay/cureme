<!-- Navigation -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">QureMy</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% session[:categories].each do |cat|%>
            <li>
              <%= link_to cat.name, search_by_category_products_path(:category_id => cat.id) %>
            </li>
        <% end %>
        <%= puts session[:user_logged_id].present? %>
        <% if session[:user_logged_id].present? %>
          <% if session[:role] == 'Admin' %>
            <li>
              <a href="<%= categories_path %>">Categories</a>
            </li>
          <% end %>
        <% end %>
        <% if !session[:user_logged_id].present? %>
          <li>
            <a href="<%= login_users_path %>">Login</a>
          </li>
        <% end %>
        <% if session[:user_logged_id].present? %>
            <li>
              <a href="<%= logout_users_path %>">Logout</a>
            </li>
        <% end %>
        <% if !session[:user_logged_id].present? %>
          <li>
            <a href="<%= register_users_path %>">Sign Up</a>
          </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if session[:user_logged_id].present? %>
            <li>
            <li><a href="#"><%= "Hi,#{session[:username]}" %> </a></li>
            <li><a href="<%= profile_users_path(:id => session[:user_logged_id]) %>">Profile</a></li>
            </li>
        <% end %>

      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container -->
</nav>