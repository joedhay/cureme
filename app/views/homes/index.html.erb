

      <div class="row">

        <div class="col-md-12">
          <div class="page-header">
            <h1>Home <!--<small>Search results for "medicines"</small>--></h1>
          </div>

        </div>
        <div class="col-md-12" style="padding-bottom: 10px">
          <div class="input-group" id="adv-search">
            <input type="text" class="form-control" placeholder="Search for Products/Medicines" />
            <div class="input-group-btn">
              <div class="btn-group" role="group">
                <div class="dropdown dropdown-lg">
                  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false"><span class="caret"></span></button>
                  <div class="dropdown-menu dropdown-menu-right" role="menu">
                    <%= form_for @product, url: search_box_homes_path(@product), html: { role: 'form' } do |f| %>
                      <div class="form-group" style="margin-bottom: 40px;">
                        <label for="filter">Filter by</label>
                        <%= f.select :category_id, @categories.collect { |c| [c.name, c.id] }, {prompt: 'Select'}, { :class => 'form-control'}  %>
                      </div>
                      <div class="form-group" style="margin-bottom: 40px;">
                        <label for="contain">Group</label>
                        <%= f.text_field :groups, :class => 'form-control' %>
                      </div>
                      <div class="form-group" style="margin-bottom: 40px;">
                        <label for="contain">Contains the words</label>
                        <%= f.text_field :description, :class => 'form-control' %>
                      </div>
                      <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-search" aria-hidden="true"></span></button>
                    <% end %>
                  </div>
                </div>
                <button type="button" class="btn btn-primary"><span class="glyphicon glyphicon-search" aria-hidden="true"></span></button>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="row">
        <% @products.each do |product| %>
        <div class="col-sm-4 col-lg-4 col-md-4">
          <div class="thumbnail">
            <% if product.item1.exists? %>
                <%= image_tag product.item1.url(:large) %>
            <% else %>
                <% if product.item2.exists? %>
                    <%= image_tag product.item2.url(:large) %>
                <% else %>
                    <% if product.item3.exists? %>
                        <%= image_tag product.item3.url(:large) %>
                    <% else %>
                        <% if product.item4.exists? %>
                            <%= image_tag product.item4.url(:large) %>
                        <% else %>
                            <img src="http://placehold.it/320x150" alt="">
                        <% end %>
                    <% end %>
                <% end %>
            <% end %>
            <div class="caption">
              <h4 class="pull-right"><%=  product.price %></h4>
              <h4> <%= link_to product.item_name, product %>
              </h4>
              <p><%=  product.description %></p>
            </div>
            <div class="ratings">
              <p class="pull-right">15 reviews</p>
              <p>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
                <span class="glyphicon glyphicon-star"></span>
              </p>
            </div>
          </div>
        </div>
<% end %>
      </div>
